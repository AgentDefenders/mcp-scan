## What

One-line summary of what this PR does.

## Why

What problem does this solve?

## Checklist

- [ ] Tests added/updated
- [ ] `pnpm test` passes
- [ ] README updated (if adding a new client or flag)
- [ ] `src/types.ts` updated (if adding a new client ID)
- [ ] `src/discovery/index.ts` updated (if adding a new discovery module)
- [ ] `src/reporters/console.ts` updated (if adding a new client display name)
